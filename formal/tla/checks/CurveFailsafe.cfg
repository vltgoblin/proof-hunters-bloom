\* DESIGN FINDING (expected to FAIL): with the bonus curve on, disableRequirement
\* waives the stake hold but matured removals keep counting in the open epoch, so stake
\* that has left the module still counts (drives the multiplier / eligibilityOf).
CONSTANTS
  Depositors = {d1, d2}
  Wallets = {w1, w2}
  Buyer = buyer
  NoD = NoD
  NoW = NoW
  MinStake = 2
  Lock = 1
  MaxDeposit = 2
  MaxTokens = 2
  MaxChallenges = 1000000
  CurveEnabled = TRUE
  CutoverMode = "twoTx"
  S8Rules = TRUE
  CoreChurn = FALSE
SPECIFICATION Spec
VIEW ViewMap
SYMMETRY Symm
INVARIANTS NoDoubleCount StakeNeverLeavesWhileCounting
CHECK_DEADLOCK FALSE
